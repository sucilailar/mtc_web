<%= form_with(model: kas_sma, local: true) do |form| %>
  <% if kas_sma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kas_sma.errors.count, "error") %> prohibited this kas_sma from being saved:</h2>

      <ul>
      <% kas_sma.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :keterangan %>
    <%= form.text_field :keterangan %>
  </div>

  <div class="field">
    <%= form.label :debit %>
    <%= form.text_field :debit %>
  </div>

  <div class="field">
    <%= form.label :kredit %>
    <%= form.text_field :kredit %>
  </div>

  <div class="field">
    <%= form.label :saldo %>
    <%= form.text_field :saldo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
