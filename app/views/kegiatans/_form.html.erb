<%= form_with(model: kegiatan, local: true) do |form| %>
  <% if kegiatan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kegiatan.errors.count, "error") %> prohibited this kegiatan from being saved:</h2>

      <ul>
      <% kegiatan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nama_kegiatan %>
    <%= form.text_field :nama_kegiatan %>
  </div>

  <div class="field">
    <%= form.label :tempat %>
    <%= form.text_area :tempat %>
  </div>

  <div class="field">
    <%= form.label :tgl %>
    <%= form.date_select :tgl %>
  </div>

  <div class="field">
    <%= form.label :biaya %>
    <%= form.text_field :biaya %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
