<%= form_with(model: kas_smp, local: true) do |form| %>
  <% if kas_smp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kas_smp.errors.count, "error") %> prohibited this kas_smp from being saved:</h2>

      <ul>
      <% kas_smp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :keterangan %>
    <%= form.text_field :keterangan %>
  </div>

  <div class="field">
    <%= form.label :debit %>
    <%= form.text_field :debit %>
  </div>

  <div class="field">
    <%= form.label :kredit %>
    <%= form.text_field :kredit %>
  </div>

  <div class="field">
    <%= form.label :saldo %>
    <%= form.text_field :saldo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
