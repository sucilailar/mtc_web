<%= form_with(model: kas_club, local: true) do |form| %>
  <% if kas_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kas_club.errors.count, "error") %> prohibited this kas_club from being saved:</h2>

      <ul>
      <% kas_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :keterangan %>
    <%= form.text_field :keterangan %>
  </div>

  <div class="field">
    <%= form.label :debit %>
    <%= form.text_field :debit %>
  </div>

  <div class="field">
    <%= form.label :kredit %>
    <%= form.text_field :kredit %>
  </div>

  <div class="field">
    <%= form.label :saldo %>
    <%= form.text_field :saldo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
