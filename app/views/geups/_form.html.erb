<%= form_with(model: geup, local: true) do |form| %>
  <% if geup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geup.errors.count, "error") %> prohibited this geup from being saved:</h2>

      <ul>
      <% geup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :geup %>
    <%= form.number_field :geup %>
  </div>

  <div class="field">
    <%= form.label :warna_string %>
    <%= form.text_field :warna_string %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
